name: travelci
description: "Plateforme de réservation de logements - MVP"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Routing
  go_router: ^14.2.0

  # Storage
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.2

  # UI Components
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.3.1
  image_picker: ^1.0.7
  intl: ^0.19.0
  table_calendar: ^3.0.9
  flutter_svg: ^2.0.9
  font_awesome_flutter: ^10.7.0

  # HTTP (for future API integration)
  http: ^1.2.0
  dio: ^5.4.0

  # Utilities
  uuid: ^4.3.3
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.8
  riverpod_generator: ^2.3.9

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
